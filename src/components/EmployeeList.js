import React, { Component } from 'react'
import {
} from 'react-native'

import {
  Card,
  CardSection
} from './widgets'

class EmployeeList extends Component {
  render() {
    return (
      <Card>
        <CardSection>
        </CardSection>
      </Card>
    )
  }
}

export default EmployeeList
